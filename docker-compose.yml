version: "3.7"

volumes:
  postgres_data: {}
  postgres_backup: {}

services:
  postgres:
    image: postgres:11
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - postgres_backup:/backups
    env_file: docker.env
    ports:
      - "5432:5432"
